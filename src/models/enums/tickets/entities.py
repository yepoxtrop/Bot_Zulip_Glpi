from enum import Enum

class Entities(Enum):
    ACS = [0, 'Acs'];
    SISTEMAS = [2, 'Sistemas'];
    FOMAG = [3, 'Fomag']; 
    AUDITORIA_MEDICA = [4, 'Auditoria Medica'];
    
# STRCUTURE
#   [id_entities, name_entities]